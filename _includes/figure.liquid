<figure>
  <img
    src="{{ src }}"
    alt="{{ alt }}"
    {% if border -%}
      class="border"
    {%- endif -%}
  >
  <figcaption class="text-center text-sm font-light mbs-4">{{ caption }}</figcaption>
</figure>
