<figure>
  <img
    src="{{ src }}"
    alt="{{ alt | escape }}"
    {% if border -%}
      class="border"
    {%- endif -%}
  >
  <figcaption class="mbs-4 text-center text-sm font-light">{{ caption }}</figcaption>
</figure>
