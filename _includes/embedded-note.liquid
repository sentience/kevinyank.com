<div id="{{ note.fileSlug }}" class="relative ps-20 is-fit max-is-max mlb-4">
  <img
    class="absolute start-0 h-14 w-14 block-start-0"
    src="/assets/images/avatar_320.jpg"
    alt="Quoted note:"
  >
  <blockquote
    class="
      bg-gray-200 dark:bg-gray-700 rounded-lg overflow-visible plb-2 pli-3
      {% #arrow -%}
      relative
      before:absolute before:-start-4 before:border-e-[1rem] before:border-transparent before:border-e-gray-200 before:mbs-4 before:border-be-[0.5rem] before:border-bs-[0.25rem] dark:before:border-e-gray-700
    "
  >
    {{- note.templateContent -}}
  </blockquote>
  <p class="text-xs text-gray-500 mbs-1">
    <a href="{{ note.url }}"
      ><time datetime="{{ note.date }}">{{ note.date | date: '%b %-d, %Y' }}</time></a
    >
    {%- if note.data.categories %}
      • in categories:
      {% for cat in note.data.categories -%}
        <span class="p-category category">{{ cat }}</span>
        {%- unless forloop.last %}, {% endunless -%}
      {%- endfor -%}
    {%- endif %}
  </p>
</div>
